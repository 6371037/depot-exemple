services:
  jenkins:
    build: .
    container_name: jenkins-offres-tp
    ports:
      - "8081:8080"      # interface web de Jenkins
    volumes:
      - jenkins-data:/var/jenkins_home
    restart: unless-stopped

volumes:
  jenkins-data: